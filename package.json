{
  "name": "finance-tracker",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "start:backend:dev": "concurrently \"npm run shared:dev\" \"npm run backend:dev\"",
    "shared:dev": "cd shared && tsc -b --watch",
    "backend:dev": "cd backend && nest start --watch"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "radash": "^12.1.1"
  }
}
